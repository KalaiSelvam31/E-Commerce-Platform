<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NexusMart - E-Commerce</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <style>
        :root {
            --primary-color: #ec4899; /* Pink */
            --primary-hover: #db2777;
            --secondary-color: #f97316; /* Orange */
            --bg-color: #111827; /* Dark Blue/Gray */
            --surface-color-rgb: 249, 250, 251; /* For glass effect */
            --text-primary: #f9fafb;
            --text-secondary: #d1d5db;
            --border-color: rgba(255, 255, 255, 0.1);
            --success-color: #22c55e;
            --error-color: #ef4444;
            --font-family: 'Poppins', sans-serif;
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.3), 0 4px 6px -4px rgb(0 0 0 / 0.3);
            --radius: 12px;
        }

        /* --- Base & Reset --- */
        * { box-sizing: border-box; }
        body {
            font-family: var(--font-family);
            background-color: var(--bg-color);
            color: var(--text-primary);
            margin: 0;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            overflow-x: hidden;
            position: relative;
        }
        
        /* Animated Gradient Background */
        body::before {
            content: '';
            position: fixed;
            top: 0; left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: var(--bg-color);
            --gradient-size: 40vmax;
            --gradient-color-1: #be185d;
            --gradient-color-2: #f97316;
            --gradient-color-3: #8b5cf6;
            background-image: 
                radial-gradient(circle at 15% 30%, var(--gradient-color-1), transparent 40%),
                radial-gradient(circle at 80% 20%, var(--gradient-color-2), transparent 40%),
                radial-gradient(circle at 50% 80%, var(--gradient-color-3), transparent 40%);
            background-size: var(--gradient-size) var(--gradient-size);
            animation: move-gradient 20s ease-in-out infinite;
            filter: blur(100px);
        }

        @keyframes move-gradient {
            0%, 100% { background-position: 0% 50%, 50% 0%, 100% 80%; }
            50% { background-position: 100% 50%, 0% 100%, 50% 0%; }
        }

        h1, h2, h3, h4, p { margin: 0; }
        button, input { font-family: inherit; color: inherit; }
        input::placeholder { color: var(--text-secondary); }

        /* --- Glassmorphism Container --- */
        .glass-surface {
            background: rgba(var(--surface-color-rgb), 0.05);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            border-radius: var(--radius);
            box-shadow: var(--shadow-lg);
        }

        /* --- Header & Navigation --- */
        .header {
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            gap: 2rem;
            position: sticky;
            top: 0;
            z-index: 1000;
            margin: 1rem auto;
            width: 95%;
            max-width: 1500px;
        }
        .header .logo {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--text-primary);
            cursor: pointer;
        }
        .search-bar {
            flex-grow: 1;
            display: flex;
        }
        .search-bar input {
            width: 100%;
            padding: 0.75rem 1rem;
            font-size: 1rem;
            border: 1px solid var(--border-color);
            background-color: rgba(var(--surface-color-rgb), 0.1);
            border-right: none;
            border-radius: var(--radius) 0 0 var(--radius);
        }
        .search-bar input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgb(236 72 153 / 0.3);
        }
        .search-bar button {
            padding: 0.75rem 1.25rem;
            border: none;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            color: var(--text-primary);
            border-radius: 0 var(--radius) var(--radius) 0;
            cursor: pointer;
            font-size: 1.25rem;
            display: flex;
            align-items: center;
        }
        .nav-actions { display: flex; align-items: center; gap: 1.5rem; }
        .nav-actions a {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transition: color 0.2s;
        }
        .nav-actions a:hover, .nav-actions a.active { color: var(--text-primary); }
        .nav-actions i { font-size: 1.5rem; }

        /* --- Main Content --- */
        .page-container {
            width: 90%;
            max-width: 1400px;
            margin: 2rem auto;
            padding: 1rem;
        }
        .page { display: none; }
        .page.active { display: block; }
        h2.page-title {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 2rem;
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* --- Login/Register Page --- */
        .auth-layout {
            display: grid;
            grid-template-columns: 1fr 1fr;
            align-items: center;
            min-height: 80vh;
            gap: 4rem;
        }
        .auth-showcase { text-align: left; }
        .auth-showcase h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            line-height: 1.2;
        }
        .auth-showcase p {
            font-size: 1.25rem;
            color: var(--text-secondary);
            max-width: 400px;
        }
        .auth-container { padding: 3rem; }
        .auth-container h2 { font-size: 1.75rem; text-align: center; margin-bottom: 2rem; }
        .auth-container form { display: flex; flex-direction: column; gap: 1.5rem; }
        .auth-container input {
            padding: 0.8rem 1rem;
            font-size: 1rem;
            border: 1px solid var(--border-color);
            border-radius: var(--radius);
            background-color: rgba(var(--surface-color-rgb), 0.1);
        }
        
        .auth-container p { text-align: center; margin-top: 1.5rem; }
        .auth-container p a { color: var(--primary-color); font-weight: 600; cursor: pointer; }

        /* --- Product Grid --- */
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        .product-card {
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
            display: flex;
            flex-direction: column;
            position: relative;
        }
        .product-card:hover { transform: translateY(-8px); }
        .product-card .badge {
            position: absolute;
            top: 1rem;
            left: 1rem;
            background-color: var(--error-color);
            color: var(--text-primary);
            padding: 0.25rem 0.75rem;
            font-size: 0.75rem;
            font-weight: 600;
            border-radius: 99px;
        }
        .product-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
        }
        .product-details {
            padding: 1rem;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            text-align: left;
        }
        .product-details h3 {
            font-size: 1rem;
            font-weight: 600;
            line-height: 1.4;
            margin-bottom: 0.5rem;
        }
        .product-rating {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-secondary);
            font-size: 0.875rem;
        }
        .product-rating .stars { color: #facc15; }
        .product-pricing { margin-top: auto; padding-top: 1rem; }
        .product-pricing .price { font-size: 1.5rem; font-weight: 700; }
        .product-pricing .original-price {
            text-decoration: line-through;
            color: var(--text-secondary);
            margin-left: 0.5rem;
        }
        
        /* --- Cart & Orders & Admin --- */
        .page-layout {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
            align-items: start;
        }
        .main-content-area, .sidebar-area {
            overflow: hidden;
        }
        .list-item {
            display: flex;
            gap: 1.5rem;
            padding: 1.5rem;
            border-bottom: 1px solid var(--border-color);
            text-align: left;
        }
        .list-item:last-child { border-bottom: none; }
        .list-item img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            border-radius: var(--radius);
        }
        .item-info { flex-grow: 1; }
        .item-info h4 { font-size: 1.1rem; font-weight: 600; margin-bottom: 0.25rem; }
        .item-info .category { color: var(--text-secondary); font-size: 0.875rem; }
        .item-actions { display: flex; align-items: center; gap: 1rem; margin-top: 1rem; }
        .quantity-control { display: flex; align-items: center; border: 1px solid var(--border-color); border-radius: var(--radius); }
        .quantity-control button { border: none; background: none; padding: 0.5rem 0.75rem; font-size: 1.25rem; cursor: pointer; }
        .quantity-control input { width: 40px; text-align: center; border: none; background: none; border-left: 1px solid var(--border-color); border-right: 1px solid var(--border-color); padding: 0.5rem 0; }
        .quantity-control input:focus { outline: none; }
        .remove-btn { color: var(--error-color); font-weight: 600; cursor: pointer; }
        .item-price-info { text-align: right; }
        .item-price-info .price { font-size: 1.25rem; font-weight: 600; }

        .summary-card { padding: 1.5rem; position: sticky; top: 120px; text-align: left; }
        .summary-card h3 { font-size: 1.25rem; margin-bottom: 1.5rem; }
        .summary-line { display: flex; justify-content: space-between; margin-bottom: 1rem; }
        .summary-line.total { font-size: 1.2rem; font-weight: 700; border-top: 1px solid var(--border-color); padding-top: 1rem; margin-top: 1rem; }
        
        .order-item-card { margin-bottom: 1.5rem; }
        .order-header { padding: 1rem 1.5rem; background-color: rgba(var(--surface-color-rgb), 0.1); display: flex; justify-content: space-between; font-size: 0.875rem; font-weight: 500; }
        .order-body { padding: 1.5rem; }
        .order-product { display: flex; gap: 1rem; align-items: center; }
        .order-product:not(:last-child) { margin-bottom: 1.5rem; }
        .order-product img { width: 80px; height: 80px; object-fit: cover; border-radius: var(--radius); }
        
        .admin-section { text-align: left; }
        .admin-section h3 { padding: 1.5rem; padding-bottom: 0; }
        .admin-form { display: flex; flex-direction: column; gap: 1rem; padding: 1.5rem; }
        .admin-form input, .admin-form .checkbox-group { padding: 0.8rem 1rem; font-size: 1rem; border: 1px solid var(--border-color); border-radius: var(--radius); background-color: var(--bg-color); }
        .checkbox-group { display: flex; align-items: center; gap: 0.5rem; }
        .edit-btn { margin-left: auto; background: none; border: none; color: var(--primary-color); cursor: pointer; font-weight: 600; }
        .update-form { display: none; width: 100%; margin-top: 1rem; }
        .empty-list-message { padding: 2rem; text-align: center; color: var(--text-secondary); }
        
        /* --- Primary Button Styles --- */
        .auth-container button,
        #checkout-btn,
        .admin-form button,
        .add-to-cart-btn {
            padding: 0.8rem;
            font-size: 1.1rem;
            font-weight: 600;
            border: none;
            border-radius: var(--radius);
            background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
            color: var(--text-primary);
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            width: 100%;
        }

        .auth-container button:hover,
        #checkout-btn:hover,
        .admin-form button:hover,
        .add-to-cart-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(236, 72, 153, 0.4);
        }

        .add-to-cart-btn {
            margin-top: 1rem;
        }

        .add-to-cart-btn:disabled {
            background: #4b5563;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }


        /* --- Loader & Modal --- */
        .loader-overlay, .modal-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            display: none; justify-content: center; align-items: center; z-index: 9998;
            background: rgba(17, 24, 39, 0.8);
            backdrop-filter: blur(5px);
        }
        .loader {
            width: 50px; height: 50px; border: 5px solid var(--surface-color);
            border-top-color: var(--primary-color); border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin { to { transform: rotate(360deg); } }
        .modal-content { text-align: center; max-width: 400px; width: 90%; padding: 2rem; }
        .modal-content i { font-size: 3rem; margin-bottom: 1rem; }
        .modal-content .icon-success { color: var(--success-color); }
        .modal-content .icon-error { color: var(--error-color); }
        .modal-content p { font-size: 1.1rem; margin-bottom: 1.5rem; }
        .modal-content button { padding: 0.6rem 1.5rem; font-weight: 600; border: none; border-radius: var(--radius); background: var(--primary-color); color: var(--text-primary); cursor: pointer; }

        /* --- Responsive --- */
        @media (max-width: 1024px) {
            .page-layout, .auth-layout { grid-template-columns: 1fr; }
            .auth-showcase { display: none; }
            .summary-card { position: static; margin-top: 2rem; }
        }
        @media (max-width: 768px) {
            .header { flex-direction: column; gap: 1rem; padding: 1rem; }
            .search-bar { width: 100%; }
            .nav-actions { width: 100%; justify-content: space-around; }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header class="header glass-surface" id="main-header" style="display: none;">
        <div class="logo" data-page="products-page">NexusMart</div>
        <div class="search-bar">
            <input type="text" id="search-input" placeholder="Search for products...">
            <button id="search-btn"><i class="ph-bold ph-magnifying-glass"></i></button>
        </div>
        <div class="nav-actions">
            <a id="admin-link" data-page="admin-page" style="display: none;"><i class="ph-bold ph-user-gear"></i> Admin</a>
            <a data-page="products-page"><i class="ph-bold ph-storefront"></i> Products</a>
            <a data-page="cart-page"><i class="ph-bold ph-shopping-cart"></i> Cart</a>
            <a data-page="orders-page"><i class="ph-bold ph-package"></i> Orders</a>
            <a id="logout-btn"><i class="ph-bold ph-sign-out"></i> Logout</a>
        </div>
    </header>
    <header class="header glass-surface" id="guest-header">
        <div class="logo">NexusMart</div>
    </header>

    <!-- Pages -->
    <main>
        <div id="auth-page" class="page">
            <div class="page-container auth-layout">
                <div class="auth-showcase">
                    <h1>Welcome to NexusMart.</h1>
                    <p id="auth-slogan">Discover millions of products, trusted by millions of users. Your one-stop shop for everything you need.</p>
                </div>
                <div class="auth-form-wrapper">
                    <div class="auth-container glass-surface" id="login-container">
                        <h2>Welcome Back!</h2>
                        <form id="login-form">
                            <input type="text" id="login-username" placeholder="Username" required>
                            <input type="password" id="login-password" placeholder="Password" required>
                            <button type="submit">Login</button>
                        </form>
                        <p>Don't have an account? <a id="show-register">Sign Up</a></p>
                    </div>
                    <div class="auth-container glass-surface" id="register-container" style="display: none;">
                        <h2>Join NexusMart</h2>
                        <form id="register-form">
                            <input type="text" id="register-username" placeholder="Username" required>
                            <input type="password" id="register-password" placeholder="Password" required>
                            <button type="submit">Create Account</button>
                        </form>
                        <p>Already have an account? <a id="show-login">Log In</a></p>
                    </div>
                </div>
            </div>
        </div>

        <div id="products-page" class="page">
            <div class="page-container">
                <h2 class="page-title">Explore Our Products</h2>
                <div id="product-list" class="product-grid"></div>
            </div>
        </div>

        <div id="cart-page" class="page">
            <div class="page-container">
                <h2 class="page-title">Your Cart</h2>
                <div class="page-layout">
                    <div id="cart-items-list" class="main-content-area glass-surface"></div>
                    <div id="cart-summary-card" class="sidebar-area summary-card glass-surface"></div>
                </div>
            </div>
        </div>

        <div id="orders-page" class="page">
            <div class="page-container">
                <h2 class="page-title">Your Order History</h2>
                <div id="order-history-list" class="order-history-list"></div>
            </div>
        </div>
        
        <div id="admin-page" class="page">
            <div class="page-container">
                <h2 class="page-title">Admin Panel</h2>
                <div class="page-layout">
                    <div class="main-content-area admin-section glass-surface">
                        <h3>Manage Products</h3>
                        <div id="manage-products-list" class="manage-products-list"></div>
                    </div>
                    <div class="sidebar-area summary-card glass-surface">
                        <h3>Add New Product</h3>
                        <form id="add-product-form" class="admin-form">
                            <input type="text" id="add-product-name" placeholder="Product Name" required>
                            <input type="number" id="add-product-price" placeholder="Price" required>
                            <input type="text" id="add-product-category" placeholder="Category" required>
                            <input type="text" id="add-product-color" placeholder="Color" required>
                             <div class="checkbox-group">
                                <input type="checkbox" id="add-product-instock" checked>
                                <label for="add-product-instock">In Stock</label>
                            </div>
                            <button type="submit">Add Product</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Loader & Modal -->
    <div class="loader-overlay" id="loader"><div class="loader"></div></div>
    <div class="modal-overlay" id="modal">
        <div class="modal-content glass-surface">
            <i id="modal-icon"></i>
            <p id="modal-message"></p>
            <button id="modal-close-btn">Close</button>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // --- CONFIG & STATE ---
            const API_URL = 'http://localhost:8080';
            const state = {
                token: localStorage.getItem('token'),
                userId: localStorage.getItem('userId'),
                role: localStorage.getItem('role'),
            };

            // --- DOM ELEMENTS ---
            const pages = document.querySelectorAll('.page');
            const mainHeader = document.getElementById('main-header');
            const guestHeader = document.getElementById('guest-header');
            const loader = document.getElementById('loader');
            const modal = {
                overlay: document.getElementById('modal'),
                icon: document.getElementById('modal-icon'),
                message: document.getElementById('modal-message'),
                closeBtn: document.getElementById('modal-close-btn'),
            };
            
            // --- UTILITY FUNCTIONS ---
            const showLoader = () => loader.style.display = 'flex';
            const hideLoader = () => loader.style.display = 'none';

            function showModal(message, type = 'success') {
                modal.message.textContent = message;
                modal.icon.className = type === 'success' ? 'ph-bold ph-check-circle icon-success' : 'ph-bold ph-x-circle icon-error';
                modal.overlay.style.display = 'flex';
            }

            // --- PAGE ROUTING ---
            function showPage(pageId) {
                pages.forEach(page => page.classList.toggle('active', page.id === pageId));
                if (state.token) {
                    mainHeader.style.display = 'flex';
                    guestHeader.style.display = 'none';
                    document.getElementById('admin-link').style.display = state.role === 'Admin' ? 'flex' : 'none';
                    if (pageId === 'products-page') initializeProductsPage();
                    if (pageId === 'cart-page') initializeCartPage();
                    if (pageId === 'orders-page') initializeOrdersPage();
                    if (pageId === 'admin-page' && state.role === 'Admin') initializeAdminPage();
                } else {
                    mainHeader.style.display = 'none';
                    guestHeader.style.display = 'flex';
                }
            }

            // --- API HELPERS ---
            async function apiRequest(endpoint, options = {}) {
                showLoader();
                try {
                    const defaultOptions = {
                        headers: {
                            'Content-Type': 'application/json',
                            ...(state.token && { 'Authorization': `Bearer ${state.token}` })
                        }
                    };
                    const response = await fetch(`${API_URL}${endpoint}`, { ...defaultOptions, ...options });
                    
                    const isJson = response.headers.get('content-type')?.includes('application/json');
                    
                    if (response.status === 404) return null;

                    const data = isJson ? await response.json().catch(() => response.text()) : await response.text();

                    if (!response.ok && response.status !== 302) { // 302 is sometimes used for redirects on success
                         let errorMessage = `Request failed with status ${response.status}`;
                         if (isJson && data && data.message) {
                             errorMessage = data.message;
                         } else if(typeof data === 'string' && data.length > 0) {
                             errorMessage = data;
                         } else if (typeof data === 'object') {
                             // Handle cases where error is an object/array without a 'message' property
                             errorMessage = "An unexpected error occurred. Please check the console.";
                             console.error("API Error Response:", data);
                         }
                         throw new Error(errorMessage);
                    }
                    return data;
                } catch (error) {
                    showModal(error.message, 'error');
                    console.error('API Request Error:', error);
                    throw error;
                } finally {
                    hideLoader();
                }
            }

            // --- PAGE INITIALIZERS & RENDERERS ---
            async function initializeProductsPage(searchTerm = '') {
                const productList = document.getElementById('product-list');
                productList.innerHTML = '';
                try {
                    const endpoint = searchTerm ? `/Search/${searchTerm}` : '/getProduct';
                    const products = await apiRequest(endpoint);
                    if (!products || products.length === 0) {
                        productList.innerHTML = `<p class="empty-list-message">No products found.</p>`;
                        return;
                    }
                    products.forEach(product => {
                        const card = document.createElement('div');
                        card.className = 'product-card glass-surface';
                        const imageUrl = `https://placehold.co/400x400/1f2937/9ca3af?text=${encodeURIComponent(product.productName)}`;
                        card.innerHTML = `
                            ${product.productPrice > 5000 ? '<div class="badge">Best Seller</div>' : ''}
                            <img src="${imageUrl}" alt="${product.productName}">
                            <div class="product-details">
                                <h3>${product.productName}</h3>
                                <div class="product-rating">
                                    <div class="stars"><i class="ph-fill ph-star"></i><i class="ph-fill ph-star"></i><i class="ph-fill ph-star"></i><i class="ph-fill ph-star"></i><i class="ph-fill ph-star-half"></i></div>
                                    <span>(1,250)</span>
                                </div>
                                <div class="product-pricing">
                                    <span class="price">₹${product.productPrice.toFixed(2)}</span>
                                    <span class="original-price">₹${(product.productPrice * 1.25).toFixed(2)}</span>
                                </div>
                                <button class="add-to-cart-btn" data-productid="${product.productId}" ${!product.inStock ? 'disabled' : ''}>
                                    ${product.inStock ? 'Add to Cart' : 'Out of Stock'}
                                </button>
                            </div>
                        `;
                        productList.appendChild(card);
                    });
                    document.querySelectorAll('.add-to-cart-btn').forEach(btn => btn.addEventListener('click', e => addToCart(e.target.dataset.productid)));
                } catch (error) { productList.innerHTML = `<p class="empty-list-message">Could not load products.</p>`; }
            }
            
            async function initializeCartPage() {
                const listContainer = document.getElementById('cart-items-list');
                const summaryContainer = document.getElementById('cart-summary-card');
                listContainer.innerHTML = '';
                summaryContainer.innerHTML = '';
                try {
                    const cartItems = await apiRequest(`/viewCart/${state.userId}`);
                    if (!cartItems || cartItems.length === 0 || typeof cartItems === 'string') {
                        listContainer.innerHTML = '<h3 class="empty-list-message">Your cart is empty.</h3>';
                        return;
                    }
                    let subtotal = 0;
                    cartItems.forEach(item => {
                        subtotal += item.price * item.quantity;
                        const itemEl = document.createElement('div');
                        itemEl.className = 'list-item';
                        itemEl.innerHTML = `
                            <img src="https://placehold.co/100x100/1f2937/9ca3af?text=${encodeURIComponent(item.product.productName)}" alt="${item.product.productName}">
                            <div class="item-info">
                                <h4>${item.product.productName}</h4>
                                <p class="category">${item.product.category}</p>
                                <div class="item-actions">
                                    <div class="quantity-control">
                                        <button class="quantity-change" data-productid="${item.product.productId}" data-change="-1">-</button>
                                        <input type="text" value="${item.quantity}" readonly>
                                        <button class="quantity-change" data-productid="${item.product.productId}" data-change="1">+</button>
                                    </div>
                                    <a class="remove-btn" data-productid="${item.product.productId}">Remove</a>
                                </div>
                            </div>
                            <div class="item-price-info"><p class="price">₹${(item.price * item.quantity).toFixed(2)}</p></div>`;
                        listContainer.appendChild(itemEl);
                    });
                    const shipping = subtotal > 0 ? 50.00 : 0;
                    const total = subtotal + shipping;
                    summaryContainer.innerHTML = `
                        <h3>Order Summary</h3>
                        <div class="summary-line"><span>Subtotal</span><span>₹${subtotal.toFixed(2)}</span></div>
                        <div class="summary-line"><span>Shipping</span><span>₹${shipping.toFixed(2)}</span></div>
                        <div class="summary-line total"><span>Total</span><span>₹${total.toFixed(2)}</span></div>
                        <button id="checkout-btn">Proceed to Checkout</button>`;
                    document.querySelectorAll('.quantity-change').forEach(btn => btn.addEventListener('click', e => {
                        const { productid, change } = e.currentTarget.dataset;
                        const qtyInput = e.currentTarget.parentElement.querySelector('input');
                        const newQuantity = parseInt(qtyInput.value) + parseInt(change);
                        if (newQuantity <= 0) {
                            removeFromCart(productid);
                        } else {
                            updateCartQuantity(productid, newQuantity);
                        }
                    }));
                    document.querySelectorAll('.remove-btn').forEach(btn => btn.addEventListener('click', e => removeFromCart(e.target.dataset.productid)));
                    document.getElementById('checkout-btn').addEventListener('click', handleCheckout);
                } catch (error) { listContainer.innerHTML = '<h3 class="empty-list-message">Could not load your cart.</h3>'; }
            }
            
            async function initializeOrdersPage() {
                const listContainer = document.getElementById('order-history-list');
                listContainer.innerHTML = '';
                try {
                    const orders = await apiRequest(`/orderDetails/${state.userId}`);
                    if (!orders || orders.length === 0) {
                        listContainer.innerHTML = '<h3 class="empty-list-message">You have no past orders.</h3>';
                        return;
                    }
                    orders.forEach(order => {
                        const orderCard = document.createElement('div');
                        orderCard.className = 'order-item-card glass-surface';
                        const productItems = order.productname.map(item => `
                            <div class="order-product">
                                <img src="https://placehold.co/80x80/1f2937/9ca3af?text=${encodeURIComponent(item.product.productName)}" alt="${item.product.productName}">
                                <div class="order-product-info"><h4>${item.product.productName}</h4><p>Quantity: ${item.quanitity}</p></div>
                            </div>`).join('');
                        orderCard.innerHTML = `
                            <div class="order-header">
                                <div><strong>Order #${order.orderid || 'N/A'}</strong> | Placed on ${new Date(order.date).toLocaleDateString()}</div>
                                <div><strong>Total: ₹${order.price.toFixed(2)}</strong></div>
                            </div>
                            <div class="order-body">${productItems}</div>`;
                        listContainer.appendChild(orderCard);
                    });
                } catch (error) { listContainer.innerHTML = '<h3 class="empty-list-message">Could not load your order history.</h3>'; }
            }
            
            async function initializeAdminPage() {
                const listContainer = document.getElementById('manage-products-list');
                listContainer.innerHTML = '';
                try {
                    const products = await apiRequest('/getProduct');
                    if (!products || products.length === 0) {
                        listContainer.innerHTML = '<p class="empty-list-message">No products to manage.</p>';
                        return;
                    }
                    products.forEach(p => {
                        const itemEl = document.createElement('div');
                        itemEl.className = 'list-item';
                        itemEl.innerHTML = `
                            <img src="https://placehold.co/100x100/1f2937/9ca3af?text=${encodeURIComponent(p.productName)}" alt="${p.productName}">
                            <div class="item-info">
                                <h4>${p.productName}</h4>
                                <p class="category">Price: ₹${p.productPrice.toFixed(2)} | In Stock: ${p.inStock}</p>
                            </div>
                            <button class="edit-btn" data-productid="${p.productId}">Edit</button>
                            <form class="update-form admin-form" data-productid="${p.productId}" style="gap: 1rem; display: none;">
                                <input type="number" value="${p.productPrice}" placeholder="New Price" required>
                                <div class="checkbox-group">
                                    <input type="checkbox" id="update-stock-${p.productId}" ${p.inStock ? 'checked' : ''}>
                                    <label for="update-stock-${p.productId}">In Stock</label>
                                </div>
                                <button type="submit">Update</button>
                            </form>
                        `;
                        listContainer.appendChild(itemEl);
                    });

                    document.querySelectorAll('.edit-btn').forEach(btn => {
                        btn.addEventListener('click', e => {
                            const form = e.target.nextElementSibling;
                            form.style.display = form.style.display === 'flex' ? 'none' : 'flex';
                        });
                    });
                    
                    document.querySelectorAll('.update-form').forEach(form => {
                        form.addEventListener('submit', e => {
                            e.preventDefault();
                            const productId = e.target.dataset.productid;
                            const price = e.target.elements[0].value;
                            const inStock = e.target.elements[1].checked;
                            handleUpdateProduct(productId, price, inStock);
                        });
                    });

                } catch(error) { listContainer.innerHTML = '<h3 class="empty-list-message">Could not load products.</h3>'; }
            }

            // --- EVENT HANDLERS ---
            async function handleLogin(e) {
                e.preventDefault();
                try {
                    const data = await apiRequest('/Login', {
                        method: 'POST',
                        body: JSON.stringify({
                            username: document.getElementById('login-username').value,
                            password: document.getElementById('login-password').value
                        })
                    });
                    state.token = data.Token;
                    state.userId = data.userId;
                    state.role = data.role;
                    localStorage.setItem('token', state.token);
                    localStorage.setItem('userId', state.userId);
                    localStorage.setItem('role', state.role);
                    showPage('products-page');
                } catch (error) { /* Modal is shown by apiRequest */ }
            }

            async function handleRegister(e) {
                e.preventDefault();
                try {
                    await apiRequest('/Register', {
                        method: 'POST',
                        body: JSON.stringify({
                            username: document.getElementById('register-username').value,
                            password: document.getElementById('register-password').value,
                            Role: "User"
                        })
                    });
                    showModal('Registration successful! Please log in.');
                    document.getElementById('show-login').click();
                } catch (error) { /* Modal is shown by apiRequest */ }
            }
            
            async function handleUpdateProduct(productId, price, inStock) {
                try {
                    await apiRequest(`/updateProducts/${productId}/${inStock}/${price}`, { method: 'POST' });
                    showModal('Product updated successfully!');
                    initializeAdminPage();
                } catch (error) { /* Modal is shown by apiRequest */ }
            }
            
            async function handleAddProduct(e) {
                e.preventDefault();
                const form = e.target;
                try {
                    await apiRequest('/addProducts', {
                        method: 'POST',
                        body: JSON.stringify({
                            productName: form.elements['add-product-name'].value,
                            productPrice: parseInt(form.elements['add-product-price'].value),
                            category: form.elements['add-product-category'].value,
                            inStock: form.elements['add-product-instock'].checked,
                            Color: form.elements['add-product-color'].value,
                        })
                    });
                    showModal('Product added successfully!');
                    form.reset();
                    initializeAdminPage();
                } catch (error) { /* Modal is shown by apiRequest */ }
            }
            
            async function addToCart(productId) {
                try {
                    await apiRequest(`/addToCart/${state.userId}/${productId}`, { method: 'POST' });
                    showModal('Product added to cart!');
                } catch (error) { /* Modal is shown by apiRequest */ }
            }
            
            async function removeFromCart(productId) {
                try {
                    await apiRequest(`/removeCart/${state.userId}/${productId}`, { method: 'POST' });
                    showModal('Product removed from cart.');
                    initializeCartPage();
                } catch (error) { /* Modal is shown by apiRequest */ }
            }
            
            async function updateCartQuantity(productId, quantity) {
                try {
                    await apiRequest(`/updateCart/${state.userId}/${productId}/${quantity}`, { method: 'POST' });
                    initializeCartPage();
                } catch (error) { /* Modal is shown by apiRequest */ }
            }

            async function handleCheckout() {
                try {
                    await apiRequest(`/Checkout/${state.userId}`, { method: 'POST' });
                    showModal('Checkout successful! Your order has been placed.');
                    showPage('orders-page');
                } catch (error) { /* Modal is shown by apiRequest */ }
            }

            // --- APP INITIALIZATION ---
            function init() {
                // Event Listeners
                document.getElementById('show-register').addEventListener('click', () => {
                    document.getElementById('login-container').style.display = 'none';
                    document.getElementById('register-container').style.display = 'block';
                    document.getElementById('auth-slogan').textContent = "Sign up to get started. It's quick and easy!";
                });
                document.getElementById('show-login').addEventListener('click', () => {
                    document.getElementById('register-container').style.display = 'none';
                    document.getElementById('login-container').style.display = 'block';
                    document.getElementById('auth-slogan').textContent = "Discover millions of products, trusted by millions of users. Your one-stop shop for everything you need.";
                });
                document.getElementById('login-form').addEventListener('submit', handleLogin);
                document.getElementById('register-form').addEventListener('submit', handleRegister);
                document.getElementById('add-product-form').addEventListener('submit', handleAddProduct);
                document.querySelectorAll('#main-header a[data-page]').forEach(link => {
                    link.addEventListener('click', () => showPage(link.dataset.page));
                });
                document.getElementById('logout-btn').addEventListener('click', () => {
                    localStorage.clear();
                    state.token = null;
                    state.userId = null;
                    state.role = null;
                    showPage('auth-page');
                });
                modal.closeBtn.addEventListener('click', () => modal.overlay.style.display = 'none');
                document.getElementById('search-btn').addEventListener('click', () => {
                    const searchTerm = document.getElementById('search-input').value;
                    initializeProductsPage(searchTerm);
                });
                document.getElementById('search-input').addEventListener('keyup', (e) => {
                    if (e.key === 'Enter') initializeProductsPage(e.target.value);
                });

                // Initial Page Load
                showPage(state.token ? 'products-page' : 'auth-page');
            }

            init();
        });
    </script>
</body>
</html>
